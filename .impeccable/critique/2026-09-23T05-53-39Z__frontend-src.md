---
target: frontend (all surfaces)
total_score: 20
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 3
target_identity: "file:D:\\College\\SOMESHIT DOWNLOADS\\shadowguard-main\\frontend\\src"
timestamp: 2026-09-23T05-53-39Z
slug: frontend-src
---
Method: dual-agent (A: design review, source-only — Chrome extension not connected · B: detector CLI + headless URL-mode scans)

## Design Health Score: 20/40 (Acceptable)
| # | Heuristic | Score | Key Issue |
|---|---|---|---|
| 1 | System status | 2 | Red "Disconnected" while connecting; perpetual AI pulse; reload shows zeros despite populated DB |
| 2 | Real world | 3 | Raw enums (LOCATION_ANOMALY, mock-fallback), "Active Tracks" jargon |
| 3 | User control | 2 | "Back to Overview" from History; no retry anywhere |
| 4 | Consistency | 2 | LiveStream bypasses tokens; one surface has 4 names |
| 5 | Error prevention | 3 | Armed reset + run lockout solid |
| 6 | Recognition | 2 | Stream cards give no clickability cue; factors reference findings by count only |
| 7 | Flexibility | 1 | No shortcuts, search, next/prev, copy |
| 8 | Minimalist | 3 | Location x3, severity x2 on Investigation |
| 9 | Error recovery | 1 | Dead-end errors; reset/scenario failures console-only |
| 10 | Help | 1 | No explanation of scoring, rules, baseline |

## Design Specificity
Only the Investigation 4-stage evidence chain is product-specific; Overview is category-interchangeable (sidebar, 4 KPIs, globe). Detector: CLI 0 findings; URL mode 128 (dashboard 63, history 23, investigation 42). Real: --text-muted #636365 at 2.9–3.4:1 (~30 hits), 10–11px text, h1 11px < h3 + h1→h3 skip, line length 99–154ch, pulsing AI dot. False positives: ai-color-palette (emerald status green), clipped-overflow (shell/globe canvas).

## Priority Issues
- [P0] Overview→Investigation is mouse-only: LiveStream cards are onClick divs (LiveStream.jsx:65), globe canvas-only. Fix: buttons + aria-label + aria-live. (/impeccable harden)
- [P1] Reload wipes Overview (AlertContext is WebSocket-only). Fix: hydrate from /api/events + /api/investigations. (/impeccable harden)
- [P1] Correlation climax unvisualized: risk_factors[].contribution, attack_scenario, supporting_anomaly_ids unrendered. Fix: contribution bar summing to score, factor↔finding linking. (/impeccable shape)
- [P1] Proportions invert demo priority (ScenarioConsole col-span-2 ≈170px); fixed w-60 sidebar breaks mobile. (/impeccable layout, /impeccable adapt)
- [P2] Severity colors used decoratively; accent-ai as focus ring; severity badge inside AI box; "mock-fallback" label. (/impeccable colorize, /impeccable clarify)

## Persona Red Flags
- Alex: no shortcuts, no History search, no copy.
- Sam: non-focusable stream/globe, unlabeled select, no live region, 1.9:1 inactive stages by color only, LiveStream motion ignores reduced-motion.
- Judge: red Disconnected + zeros on first paint, thesis not stated on Overview, Run cramped, payoff below fold, unpkg globe texture fails offline.

## Minor Observations
Anomalies KPI counts events not anomalies; "Awaiting telemetry" for empty filter; float scores unformatted; no timezone on timestamps; wrong icon for access_pattern; anomaly evidence never shown.

## Questions
- Would a travel arc inside Impossible Travel beat the globe?
- Why does the product end at a bulleted list?
- Is the Scenario Console a prop or the stage?
